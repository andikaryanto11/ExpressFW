html
     head 
     body 
          form(action='/office/test/doupload' method="POST" enctype="multipart/form-data")
               input(type="file" name="file" multiple)
               input(type="file" name="img")
               button(type="submit" value="upload") Upload